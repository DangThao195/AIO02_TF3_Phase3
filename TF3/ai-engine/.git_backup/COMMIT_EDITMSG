feat(ai-engine): close C1-C6 contract gaps + multi-window robust z-score latency

Contract gap closures (audited C1-C6 checklist):
- C4.6 local_matcher: offline incident diagnosis fallback when LLM/Bedrock down;
  INC-2 hard-bound to action=none (never restart single-replica cart). Wired into RCA.
- C6.8 audit_log: append-only incidents/<id>/actions.jsonl at every state transition.
- C6.6/6.11 verify_loop + remediation: 5-min post-exec SLI verify -> auto-rollback on
  no-recovery; timeout invariant (>300s -> result "timeout"); escalate on rollback failure.
- C6.10/6.9 audit_report + scripts/audit-check.sh: machine-checkable invariants (exit 1
  on violation) + weekly Ops-Review report.
- C5.4/5.5/5.6 cost_report: weekly cost/reports/YYYY-Www.md, AI_BUDGET_WEEKLY_USD budget,
  80% warning / 100% critical threshold alerts (source_layer=cost).
- C3 rca_assistant: EvidencePack.write() persists draft pack to incidents/<id>/.
- C2.9 alert_emitter: reconcile() emits status:"resolved" when a fingerprint stops firing.
- C1.6 grafana: AI Engine Health dashboard (blind, breaker, alerts, detect latency, budget).

Advanced detection (24-48h phase):
- detector_latency: MultiWindowLatencyDetector — robust z-score (median+MAD) on p95 latency,
  alerts only when z>3 breaches BOTH long(30m) AND short(5m) windows (MWMBR-style, kills
  transient spikes). Capped at WARNING (C2.3), never critical.

Tests: +34 (100 total passing). New modules lint clean.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
